# WeatherAPI key from https://www.weatherapi.com/
API_KEY=

# Server port (Render/Railway set this automatically)
PORT=5000

# Comma-separated allowed origins in production, e.g. https://your-app.com
# Leave empty in development to allow all local origins
CLIENT_URL=

# Set to "production" when deploying
NODE_ENV=development
