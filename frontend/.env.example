# Leave empty in development (Vite proxies /api to the backend).
# In production, leave empty if frontend and API share the same origin.
# Set only if the API lives on another domain, e.g. https://api.example.com
VITE_API_URL=
